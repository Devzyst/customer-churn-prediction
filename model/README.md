# Model artifacts

Esta pasta recebe os artefatos gerados pelo treinamento:

- `churn_model.joblib`: pipeline treinado com pré-processamento e classificador;
- `metrics.json`: métricas e metadados do treinamento.

Execute `python -m src.train` para gerar ou atualizar esses arquivos localmente.
